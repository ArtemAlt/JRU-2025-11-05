import java.util.Map;

public class DatabaseConnection {
    private static String url;
    private static Map<String, String> credentials;

    static {
        System.loadLibrary("someLibrary");
        // connect to chat bot
        // for () {get cred credentials.add()}
    }
}
